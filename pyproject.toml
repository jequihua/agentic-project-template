[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-ml-project-template"
version = "0.1.0"
description = "Starter template for artifact-first AI-assisted ML workflows"
requires-python = ">=3.10"
