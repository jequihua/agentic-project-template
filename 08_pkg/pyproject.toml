[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "template-pkg"
version = "0.1.0"
description = "Reusable package extracted from the project"
requires-python = ">=3.10"
